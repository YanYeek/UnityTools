namespace YanYeek
{
    public static class QueryDefine
    {
        public const string Create_Table_Begin = "CREATE TABLE IF NOT EXISTS {0} (id INTEGER PRIMARY KEY AUTOINCREMENT NOT NULL UNIQUE, Data TEXT";
        
    }
}